fastmcp>=0.2.0
edge-tts>=6.1.0
pynput>=1.7.6

# Optional STT dependency (install separately if needed)
# faster-whisper>=1.1.0  # Requires Python <=3.13 (onnxruntime limitation)
# Alternative: Use whisper.cpp Python bindings or wait for onnxruntime Python 3.14 support
